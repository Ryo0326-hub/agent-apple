# Submission asset manifest

Only public, credential-free media belongs in this directory. Before adding an image or video, inspect every visible terminal, browser tab, tooltip, expanded JSON field, and filename.

## Prepared assets

| Filename | Purpose | Required content |
| --- | --- | --- |
| `thetatrap-cover-1200x630.png` | LabLab cover and GitHub social preview | ThetaTrap title, value proposition, and paper/MCP/Qwen label |
| `thetatrap-cover-source.png` | Editable cover source | Full-resolution generated cover before exact-size export |
| `thetatrap-visual.png` | Deck and one-page visual | Text-free bounded-volatility illustration |
| `public-dashboard-desktop.png` | Submission gallery | Public read-only status and system-health evidence |
| `public-dashboard-evidence.png` | Submission gallery | Qwen, order, position, equity, and limitations area |
| `public-dashboard-mobile.png` | Responsive proof | Public page at 390 by 844 with no horizontal overflow |

The dashboard screenshots currently show the local disarmed readiness state. Replace or supplement them with sanitized competition evidence only after broker reconciliation.

## Publication rules

- Never show an API key, secret, token, environment file, full account UUID, Droplet IP, SSH command, or unredacted Compose configuration.
- Use the public read-only dashboard for public screenshots. The private operator dashboard contains the kill switch and must not appear with an accessible URL.
- Label replay screenshots `SIMULATION / NO BROKER MUTATION`.
- Label live account evidence `ALPACA PAPER` and data `BASIC INDICATIVE`.
- Do not add a final-equity image until it matches the exported final report.
- Optimize images for the web and preserve an editable source outside the public repository when appropriate.
